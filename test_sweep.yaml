program: paired_codebook_ae.calculate_metrics
method: grid
metric:
  goal: maximize
  name: "Validation/Total"
parameters:
  experiment.log_training_graph:
    value: false
  metrics.metrics_dir:
    values:
    


command:
  - python
  - -m
  - ${program}
  - ${args_no_hyphens}
